---
import { Loader2 } from "lucide-react"
import { Button } from "@/components/ui/button"

import '@/styles/globals.css'
import '@/styles/news.css'
import '@/styles/baseTopPageTextBox.css'

import BaseLayout from '../layouts/InfoPageLayout.astro';
import Greeting from '../components/Greeting.jsx';

import {
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table"

const companyName = "DELC";
const pageTitle = "COMPANY";
const pageTitleRuby = "会社情報";
const tabTitle = "会社情報";

const companyInformation = "沿革";
const information = "HISTORY";

const destinationURLs = [
  {url: "company",ruby: "会社情報"},
  {url: "history",ruby: "沿革"}
];

const menus = [
  {url: "company",text: "会社情報"},
  {url: "location",text: "所在地"},
  {url: "history",text: "沿革"}
];

const show=2;
---

<BaseLayout companyName={companyName}
pageTitle={pageTitle}
pageTitleRuby={pageTitleRuby}
tabTitle={tabTitle}

menus={menus}
menuShow={show}
anchor={false}

destinationURLs={destinationURLs}
>

<div class = "main-title-block">
    <div class = "main-jp-title">{companyInformation}</div>
    <div class = "main-title">{information}</div>
</div>

<Table>
    <TableHeader>
        <TableRow disableHover>
          <TableCell>2018</TableCell>
          <TableCell>8月</TableCell>
          <TableCell>東京都豊島区南池袋に移転</TableCell>
        </TableRow>
      </TableHeader>
      <TableBody>
        <TableRow disableHover>
          <TableCell>2015</TableCell>
          <TableCell>2月</TableCell>
          <TableCell>東京都豊島区東池袋に移転</TableCell>
        </TableRow>
        <TableRow disableHover>
          <TableCell>2014</TableCell>
          <TableCell>4月</TableCell>
          <TableCell>株式会社デルク設立</TableCell>
        </TableRow>
      </TableBody>

</Table>

</BaseLayout>