---
import BaseLayout from './BaseLayout.astro'
import '../styles/delcGlobal.css'
import '../styles/delcInfoGlobal.css'

import { Toaster } from '@/components/ui/toaster'

import BreadCrumb from '@/components/BreadCrumb.astro'

const { pageTitle, tabTitle, pageTitleRuby, destinationURLs } = Astro.props

const NewsURL = [{ url: 'news', ruby: 'お知らせ' }]

const merged = [...NewsURL, ...destinationURLs];
---

<BaseLayout {...Astro.props} destinationURLs={merged}>
    <slot />
</BaseLayout>

<script>
  import '../scripts/menu.js'
</script>
