---
import { cn } from '@/lib/utils'
import '@/styles/delcGlobal.css'
const { color } = Astro.props
---

<div class="relative z-10 h-[21rem]">
  <a href="/" class="company-header group w-40">
    <div
      class={cn(
        'company-branding',
        color || 'bg-foreground',
        // 'group-hover:bg-accent transition-all',
      )}
    >
    </div>
    <div
      class={cn(
        'company-logo',
        color || 'bg-foreground',
        'group-hover:-translate-y-[1px] transition-all',
      )}
    >
    </div>
  </a>
</div>
